// @flow

/*::
import type {
    RequestParams,
    GatewayHeaders,
    OutsideRequestParams,
    MtgResponse,
    AuthHeaders,
    CreateRequestOptions,
    GatewayConfig,
    AccessToken
} from './types'
*/

const x/*: {| ...CreateRequestOptions, gatewayConfig: GatewayConfig |} => (RequestParams => Promise<MtgResponse>) */ = require('./1');
